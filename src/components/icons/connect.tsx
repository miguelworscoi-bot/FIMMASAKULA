import React from 'react';
import { Cable, Link2, Zap, LucideProps } from 'lucide-react';

export const Connect: React.FC<LucideProps> = (props) => <Cable {...props} />;
export const LinkIcon: React.FC<LucideProps> = (props) => <Link2 {...props} />;
export default Connect;
